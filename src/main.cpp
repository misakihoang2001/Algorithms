#include "statusbar.h"

int main() {
	sf::VideoMode def = sf::VideoMode::getDesktopMode();
	const int FPS = 60;
	const int WIDTH = def.width;
	const int HEIGHT = def.height;
	const int BAR_H = 0.05 * HEIGHT;
	const int DATA_SIZE = 200;

	sf::RenderWindow win(def, )
}